library("actel")

# set your work directory to the 2017 data folder
setwd("your/path/to/this/folder") # modify this line before running!

load("../bush_transition_layer.RData")
distancesMatrix(tl, coord.x = "x.29902", coord.y = "y.29902")
y # save distance matrix as a distances.csv file

bush17 <- migration(tz = "Europe/London", inactive.error = 5, success.arrays = c("A6", "A7"), report = TRUE)
y # save strays?
y # invalidate/expand moves in R64K-2764?
1:23
y # confirm?
y # invalidate/expand moves in R64K-2776?
3:17
y # confirm?
n # invalidate more?
y # invalidate/expand moves in R64K-2791?
1
y # confirm?
n # invalidate more?
n # invalidate/expand moves in R64K-2791?
y # invalidate/expand moves in R64K-2792?
1
y # confirm?
n # invalidate more?
y # invalidate/expand moves in R64K-2811?
1:69
y # confirm?
y # invalidate/expand moves in R64K-2849?
3
y # confirm?
n # invalidate more?
y # save results?
